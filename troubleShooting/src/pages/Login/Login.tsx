import * as S from "./Login.styles.tsx";
import LoginForm from "./LoginForm/LoginForm.tsx";

const Login = () => {
  return (
    <S.LoginContainer>
      <LoginForm />
    </S.LoginContainer>
  );
};

export default Login;
